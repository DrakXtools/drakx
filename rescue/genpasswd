#!/usr/bin/perl

system "stty -echo";
print STDERR "Give a password: ";
chomp($word = <STDIN>);
print STDERR "\n";
system "stty echo";

print crypt($word, join('', ('.', '/', 0..9, 'A'..'Z', 'a'..'z')[rand 64, rand 64]));
print "\n";
