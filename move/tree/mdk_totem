#!/usr/bin/perl

system('mdk_move_loop', 'to_memory', 'always', 'totem');

my $totem_pid = $$;

if (my $pid = fork()) {
    $ENV{MDKMOVE} = $pid;
    exec 'totem';
} else {
    exec 'sudo', 'mdk_behind_totem', '--totem-pid', $totem_pid;
}
