#!/usr/bin/perl

$width = 400;
$height = 270;
$icon = 0;
$icon_dx = 0;
$icon_dy = 0;
@data = (
	 [ _("Turn your machine into a reliable server"), 0, 10, 400, 20, 1],
	 [ _("Transform your machine into a powerful server in a few clicks of your mouse: Web server, mail, firewall, router, file and print server..."), 0, 50, 400, 60, 0],
#	 [ 'text', $x, $y, $area_width, $area_height ],
	);
