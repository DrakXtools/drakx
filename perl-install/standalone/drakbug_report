#!/usr/bin/perl

use lib qw(/usr/lib/libDrakX);

use common;
use any;

my %other = (
    'rpm -qa' => join('', sort `rpm -qa`),
    'mandrake version' => mdk_rel(),
    'df' => join('', `df`),
);

print any::report_bug('', %other);
