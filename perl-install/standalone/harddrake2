#!/usr/bin/perl -w

use lib qw(/usr/lib/libDrakX);
use standalone;
use harddrake::ui;

if ("@ARGV" =~ /--help|-h/) {
    print "Harddrake 2\n", $license, "\nUsage: harddrake [-h|--help]\n";
    exit;
}
$::isStandalone=1;

harddrake::ui->new;
